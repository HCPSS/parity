{
  "scripts": {
    "build:sass": "node-sass -r scss -o css",
    "watch:sass": "npm run build:sass && npm run build:sass -- -w"
  },
  "devDependencies": {
    "node-sass": "^9.0.0"
  },
  "dependencies": {
    "bulma": "^0.9.4"
  }
}
