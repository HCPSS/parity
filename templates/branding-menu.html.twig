<nav class="column is-narrow">
  <div class="columns has-text-success is-size-4 is-uppercase has-text-weight-bold">
    {% for item in items %}
      <div class="column">
        {{ link(item.title, item.url) }}
      </div>
    {% endfor %}
  </div>
</nav>
